version: "3"
services:
  api-server:
    container_name: urlshortener
    build:
      context: .
      dockerfile: api.Dockerfile
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/shortener?enabledTLSProtocols=TLSv1.2
    ports:
      - "8080:8080"
    networks:
      - urlshortenersystem-mysql-network
    depends_on:
      - api-db

  api-db:
    container_name: shorten
    build:
      context: .
      dockerfile: db.Dockerfile
    networks:
      - urlshortenersystem-mysql-network
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=shorten
      - MYSQL_USER=root
      - MYSQL_PASSWORD=root
    ports:
      - '3306:3306'

networks:
  urlshortenersystem-mysql-network: